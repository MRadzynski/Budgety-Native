import { Text } from 'react-native';

interface DrawerProps {
  navigation: any;
}

const SettingsScreen = ({ navigation }: DrawerProps) => {
  return <Text>SettingsScreen</Text>;
};

export default SettingsScreen;
