import { Text } from 'react-native';

interface DrawerProps {
  navigation: any;
}

const ExpensesIncomeScreen = ({ navigation }: DrawerProps) => {
  return <Text>ExpensesIncomeScreen</Text>;
};

export default ExpensesIncomeScreen;
