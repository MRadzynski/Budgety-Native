import { Text } from 'react-native';

interface DrawerProps {
  navigation: any;
}

const ExchangeScreen = ({ navigation }: DrawerProps) => {
  return <Text>ExchangeScreen</Text>;
};

export default ExchangeScreen;
